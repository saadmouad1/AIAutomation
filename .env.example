# PostgreSQL — provide your own connection string
DATABASE_URL="postgresql://USERNAME:PASSWORD@localhost:5432/aurivo_dev"

# Auth — generate with: openssl rand -base64 32
NEXTAUTH_SECRET=secret-needs-to-be-replaced
NEXTAUTH_URL=http://localhost:3000

# App
NODE_ENV=development
